package token

const (
	// PrismaTokenEnv env var
	PrismaTokenEnv = "PRISMA_TOKEN"

	// ApoctlTokenEnv env var
	ApoctlTokenEnv = "APOCTL_TOKEN"

	// EnforcerdTokenEnv env var
	EnforcerdTokenEnv = "ENFORCERD_TOKEN"
)
